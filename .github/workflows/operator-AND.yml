name: Logical AND Operator Example

on:
  workflow_dispatch:
    inputs:
      env_name:
        description: 'Environment name'
        required: true
        default: 'dev'

jobs:
  logical-and:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'ran130987wan' && inputs.env_name == 'dev' }}
    steps:
      - run: echo "👤 ran130987wan is deploying to dev"
